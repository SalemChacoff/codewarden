[workspace]
members = [
    "crates/codewarden-core",
    "crates/codewarden-cli",
]
resolver = "2"

# ── Shared metadata inherited by every crate ──────────────────────────────────
[workspace.package]
version     = "0.1.0"
edition     = "2024"
license     = "MIT"
authors     = []
repository  = ""

# ── Workspace-level dependencies (pinned here, referenced with .workspace = true)
[workspace.dependencies]
codewarden-core = { path = "crates/codewarden-core" }
